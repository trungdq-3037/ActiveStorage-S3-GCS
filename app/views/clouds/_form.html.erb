<%= form_with(model: cloud) do |form| %>
  <% if cloud.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(cloud.errors.count, "error") %> prohibited this cloud from being saved:</h2>

      <ul>
        <% cloud.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
